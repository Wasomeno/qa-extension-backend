{
  "id": "eb733e3a",
  "title": "Fix critical and high severity issues in test runner",
  "tags": [
    "bugfix",
    "runner",
    "playwright"
  ],
  "status": "completed",
  "created_at": "2026-04-21T05:06:05.193Z",
  "assigned_to_session": "019dae30-a096-70fd-b046-16039459d00d"
}

## Fixed Issues:

### Critical Issues Fixed:
1. ✅ **No console error capture** - Added `OnConsole`, `OnPageError`, `OnRequestFailed`, and `OnResponse` handlers to capture all browser errors
2. ✅ **Headless mode detection** - Added UserAgent spoofing and browser context options to hide headless browser

### High Issues Fixed:
1. ✅ **No UserAgent spoofing** - Added realistic Chrome UserAgent string
2. ✅ **No page state debugging** - Added detailed logging of URL, title, page content, and React root state when selectors fail

## Changes Made to `runner.go`:

1. **Browser Context Options** (lines 86-101):
   - Added UserAgent spoofing with Chrome 120 string
   - Added locale, timezone, hasTouch, isMobile, deviceScaleFactor options
   - Makes headless Chromium look like a real Chrome browser

2. **Console/Error Capture** (lines 108-132):
   - OnConsole: Captures console.log, console.error, console.warn
   - OnPageError: Captures uncaught JavaScript errors
   - OnRequestFailed: Captures failed network requests
   - OnResponse: Captures HTTP 4xx/5xx responses

3. **Page State Debugging** (lines 635-669):
   - Logs current URL when selectors fail (detects redirects)
   - Logs page title
   - Logs page body preview (first 500 chars)
   - Checks if React #root has content or is empty

4. **Navigation URL Logging** (lines 707-715):
   - Logs final URL after navigation
   - Warns if page redirected from requested URL
   - Logs page title for debugging

5. **Same fixes applied to RunTestsChained** function for consistency

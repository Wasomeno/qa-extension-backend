{
  "id": "dc170e45",
  "title": "Implement Fix with Agent feature",
  "tags": [
    "feature",
    "agent",
    "gitlab",
    "claude"
  ],
  "status": "completed",
  "created_at": "2026-04-18T05:16:03.165Z"
}

Implement the "Fix with Agent" feature as per PLAN-agent-fix-issue.md

**Files to create:**
1. `agent/claude_runner.go` - Core orchestrator for git clone, spawn Claude, handle stop hook, push, create MR
2. `agent/system_prompt_fix.go` - System prompt for the fix agent
3. `agent/claude_hooks.go` - Per-session Claude Code hooks configuration
4. `agent/tools_gitlab_write.go` - GitLab write tools (branch, commit, MR)
5. `routes/agent_fix.go` - `POST /agent/fix-issue` SSE route handler

**Files to modify:**
6. `routes/agent.go` - Register `/fix-issue` route
7. `main.go` - Add the new route

**Implementation order:**
1. GitLab write tools
2. System prompt and hooks
3. Core runner/orchestrator
4. HTTP route handler
5. Register route

{
  "id": "69d03af4",
  "title": "Pi Runner Implementation Complete",
  "tags": [
    "agent",
    "pi-runner",
    "implementation"
  ],
  "status": "open",
  "created_at": "2026-04-19T04:47:21.585Z"
}

# Pi Runner Implementation Summary

## What Was Implemented

### 1. New File: `agent/pi_runner.go` (~700 lines)
- Complete Pi coding agent runner
- RPC mode communication with Pi
- Local and remote (SSH) execution support
- JSON-RPC protocol client
- Event streaming integration

### 2. Modified: `agent/claude_runner.go`
- Added `RunFixAgent()` router function
- Renamed original to `RunFixWithClaude()`
- No changes to existing Claude logic

### 3. Modified: `routes/agent_fix.go`
- Added `runner` field to request payload
- Added validation for runner value
- Updated response to include runner

### 4. New Doc: `docs/PI_RUNNER_IMPLEMENTATION.md`
- Complete documentation of the implementation
- API changes, configuration, testing instructions

## How to Use

### Default (Claude):
```bash
curl -X POST http://localhost:8080/agent/fix-issue \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"project_id": 123, "issue_iid": 456}'
```

### With Pi:
```bash
curl -X POST http://localhost:8080/agent/fix-issue \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"project_id": 123, "issue_iid": 456, "runner": "pi"}'
```

## Prerequisites

1. Install Pi: `npm install -g @mariozechner/pi-coding-agent`
2. Set API keys: `ANTHROPIC_API_KEY` or `OPENAI_API_KEY`

## Build Status

✅ Code compiles successfully
✅ No breaking changes to existing functionality
✅ Both runners coexist independently

## Next Steps

- [ ] Test with real GitLab instance
- [ ] Test with Pi installed
- [ ] Add integration tests
- [ ] Monitor and refine RPC protocol handling

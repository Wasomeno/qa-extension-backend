{
  "id": "2ec46fd9",
  "title": "Add inline run results to AutomationTest with video, step results, log",
  "tags": [],
  "status": "open",
  "created_at": "2026-04-30T08:18:39.995Z"
}

## Plan

### Backend
1. `internal/models/test_scenario.go` — Add `VideoURL`, `StepResults`, `Log` to `AutomationTest`
2. `agent/runner.go` — Track `RunDurationMs`, ensure `VideoURL`/`StepResults`/`Log` flow through
3. `agent/tools_recording.go` — After batch/single runs, update scenario `AutomationTest` with results, save scenario
4. `agent/tool_executor.go` — Same for direct execution paths

### Frontend
5. `qa-webapp/src/types/test-scenario.ts` — Add new fields to `AutomationTest`
6. `qa-webapp/src/pages/test-scenarios/components/scenario-detail.tsx` — Show video, step results, log in `LastRunPanel`

{
  "id": "2ec46fd9",
  "title": "Add inline run results to AutomationTest with video, step results, log",
  "tags": [],
  "status": "completed",
  "created_at": "2026-04-30T08:18:39.995Z"
}

## Completed ✅

### Backend
- `internal/models/test_scenario.go` — Added `VideoURL`, `StepResults`, `Log` to `AutomationTest`
- `internal/models/manual_recording.go` — Added `RunDurationMs` to `TestResult`
- `agent/runner.go` — Track run duration via defer + named return
- `agent/tools_recording.go` — After batch/single runs, update scenario `AutomationTest` with result, save scenario
- `agent/tool_executor.go` — Same for direct execution paths

### Frontend
- `src/types/test-scenario.ts` — Added `videoUrl`, `stepResults`, `log` to `AutomationTest`
- `src/pages/test-scenarios/lib/mock-scenario-data.ts` — Same
- `src/pages/test-scenarios/components/scenario-detail.tsx` — `LastRunPanel` now shows:
  - Video player (if `videoUrl`)
  - Step-by-step results list (pass/fail per step)
  - Run log (pre-formatted)
  - Failure details (existing)

Both repos committed and pushed.

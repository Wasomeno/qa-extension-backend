{
  "id": "3268a29a",
  "title": "Refactor: Move generated tests into AutomationTest.Steps, rename TestRecording to ManualRecording",
  "tags": [],
  "status": "completed",
  "created_at": "2026-04-30T03:01:45.448Z"
}

## Completed

### 1. Model Changes ✅
- Renamed `TestRecording` → `ManualRecording`, `RecordingSummary` → `ManualRecordingSummary`
- Removed `SourceType`/`SourceID` from manual recording structs
- Added `Steps []RecordingStep` to `AutomationTest`
- Removed `RecordingID` from `AutomationTest`
- Added `TestRun` runtime struct for runner.go
- Added `GeneratedAutomation` struct for the generation pipeline

### 2. Agent/Generation ✅
- Repurposed `save_test_recording` tool → updates scenario's `AutomationTest` directly via `saveScenarioToRedis`
- Updated `RunAgentForTestGeneration` pipeline to work with `GeneratedAutomation`
- Updated `runTestScenario` to read steps from `AutomationTest.Steps`
- Updated `RunTest` signature to accept `*models.TestRun`
- Updated `tool_executor.go` paths

### 3. Handlers ✅
- `handlers/recording.go`: Uses `ManualRecording`, removed source_type/source_id filter/indexing
- `handlers/test_scenario.go`: 
  - Saves generated steps into scenario `AutomationTest` instead of separate Redis recordings
  - Removed cascade delete of generated recordings in `DeleteScenario` and `BulkDeleteScenarios`
  - Removed `RecordingID` cleanup in `DeleteTestCase`

### 4. Services ✅
- `services/scenario_converter.go`: Replaced `LinkRecording`/`LinkRecordingToTestCase` with `LinkAutomation`
- `services/test_generator.go`: Returns `[]GeneratedAutomation` instead of `[]TestRecording`

### 5. Scripts & misc ✅
- Updated `scripts/*.go` to use `models.TestRun`
- All packages compile successfully
